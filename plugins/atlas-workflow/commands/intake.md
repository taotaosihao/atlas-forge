---
description: Use the Atlas intake/grilling flow to stress-test a plan or design before building
argument-hint: '[request]'
allowed-tools: Read, Glob, Grep, Bash(node:*), Bash(git:*), AskUserQuestion
---

Raw slash-command arguments:
`$ARGUMENTS`

Use the `intake` skill for this request. Follow its grilling protocol and routing rules exactly as written there; this command is only the Claude Code entrypoint into that skill.
