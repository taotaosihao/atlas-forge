---
description: Use the Atlas intake flow for an explicit interview or pressure-test, or a material unresolved plan/design decision
argument-hint: '[request]'
allowed-tools: Read, Glob, Grep, Bash(node:*), Bash(git:*), AskUserQuestion
---

Raw slash-command arguments:
`$ARGUMENTS`

Use the `intake` skill for this request. Follow its targeted pressure-test and routing rules exactly as written there; this command is only the Claude Code entrypoint into that skill.
