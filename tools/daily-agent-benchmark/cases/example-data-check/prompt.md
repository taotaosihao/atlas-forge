# Task

Audit the synthetic report snapshot in `snapshot/weekly_signups.csv`.

Check whether the `TOTAL` row's `reported_rate` matches the segment rows. Return:

- the recomputed total signup rate;
- whether the reported total is correct;
- the size of the discrepancy;
- the smallest correction you would make to the report.

Use only the snapshot. Do not infer missing data or use external sources.
